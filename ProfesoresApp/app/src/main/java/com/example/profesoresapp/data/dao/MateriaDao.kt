package com.example.profesoresapp.data.dao
/*
import androidx.room.*
import androidx.room.OnConflictStrategy.IGNORE
import com.example.alumnosapp.domain.model.Materia

@Dao
interface MateriaDao {

    @Query("SELECT * FROM Materia WHERE name LIKE :filter")
    fun getMateriasByName(filter: String): List<Materia>

    @Insert(onConflict = IGNORE)
    fun saveMaterias(materias: List<Materia>): List<Long>

    @Update
    fun updateMaterias(materia: Materia): Int
}*/